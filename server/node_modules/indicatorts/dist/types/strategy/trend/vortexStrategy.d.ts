import { Asset } from '../asset';
import { Action } from '../action';
import { VortexConfig } from '../../indicator/trend/vortex';
/**
 * Vortex strategy.
 * @param asset asset object.
 * @param config configuration.
 * @return strategy actions.
 */
export declare function vortexStrategy(asset: Asset, config?: VortexConfig): Action[];
