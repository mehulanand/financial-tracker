import { Asset } from '../asset';
import { Action } from '../action';
import { ABConfig } from '../../indicator/volatility/accelerationBands';
/**
 * Acceleration bands strategy function.
 *
 * @param asset asset object.
 * @param config configuration.
 * @return strategy actions.
 */
export declare function abStrategy(asset: Asset, config?: ABConfig): Action[];
export { abStrategy as accelerationBandsStrategy };
